<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Practical_1" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>
    <style name="CalculatorButton">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:textSize">30sp</item>
        <item name="android:gravity">center</item>
        <item name="android:backgroundTint">#000</item>
        <item name="android:background">@drawable/round_button_selector</item>
    </style>

    <!-- Style for operator buttons (if you want a different color, you can override properties here) -->
    <style name="OperatorButton" parent="CalculatorButton">
        <item name="android:background">@drawable/round_button_selector</item>
        <item name="android:backgroundTint">#000</item>
        <!-- Optional: If you want a different text color for operators -->
        <item name="android:textColor">#FFFFFF</item>
    </style>

    <!-- Style for the calculator title -->
    <style name="CalculatorDisplayText">
        <item name="android:textSize">28sp</item>
        <item name="android:textStyle">bold</item>
    </style>
</resources>